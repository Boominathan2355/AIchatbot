{
  "name": "aichatbot",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chatbot powered by Gemini API with Document Analysis Agent",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && rm -rf node_modules package-lock.json && npm install && cd ../frontend && rm -rf node_modules package-lock.json && npm install",
    "build": "npm run install:all && npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "node backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
